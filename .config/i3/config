# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

set $left h
set $right l
set $down j
set $up k

# General Configuration
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango: Hack Nerd Font 8
hide_edge_borders both

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec mate-terminal
# kill focused window
bindsym $mod+Shift+q kill
bindsym $mod+F4 kill

# start rofi (a program launcher)
bindsym $mod+d exec rofi -show run -theme base16oceancustom 
bindsym $mod+Shift+d exec rofi -show window -theme base16oceancustom
bindsym $mod+Shift+s exec echo "" | rofi -dmenu -theme base16oceancustom1line -p "Search" | xargs -I{} xdg-open "https://www.google.com/search?q={}"
bindsym $mod+Shift+w exec echo "" | rofi -dmenu -theme base16oceancustom1line -p "Add to TickTick" | xargs -I{} ticktask {}
# change focus
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+b split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# select last active floating container
bindsym $mod+m focus floating;
bindsym $mod+Shift+m focus floating; mode "movefloat"

# focus the parent container
bindsym $mod+a focus parent

mode "movefloat" {
    bindsym Up move up 20px
    bindsym Left move left 20px
    bindsym Down move down 20px
    bindsym Right move right 20px

    bindsym $up move up 20px
    bindsym $left move left 20px
    bindsym $down move down 20px
    bindsym $right move right 20px

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# focus the child container
#bindsym $mod+d focus child

#set $workspace1  "1:1 "
#set $workspace2  "2:2 "
#set $workspace3  "3:3 "
#set $workspace4  "4:4 "
#set $workspace5  "5:5 "
#set $workspace6  "6:6 "
#set $workspace7  "7:7 "
#set $workspace8  "8:8 "
#set $workspace9  "9:9 "
#set $workspace10 "10:0 "
#set $workspace11 "- "
#set $workspace12 "+ "

set $workspace1  "1:1"
set $workspace2  "2:2"
set $workspace3  "3:3"
set $workspace4  "4:4"
set $workspace5  "5:5"
set $workspace6  "6:6"
set $workspace7  "7:7"
set $workspace8  "8:8"
set $workspace9  "9:9"
set $workspace10 "10:0"
set $workspace11 "11:-"
set $workspace12 "12:+"

# switch to workspace
bindsym $mod+1 workspace $workspace1
bindsym $mod+2 workspace $workspace2
bindsym $mod+3 workspace $workspace3
bindsym $mod+4 workspace $workspace4
bindsym $mod+5 workspace $workspace5
bindsym $mod+6 workspace $workspace6
bindsym $mod+7 workspace $workspace7
bindsym $mod+8 workspace $workspace8
bindsym $mod+9 workspace $workspace9
bindsym $mod+0 workspace $workspace10
bindsym $mod+minus workspace $workspace11
bindsym $mod+equal workspace $workspace12

bindsym $mod+Ctrl+Right workspace next
bindsym $mod+Ctrl+Left workspace prev

bindsym $mod+Ctrl+$right workspace next
bindsym $mod+Ctrl+$left workspace prev

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $workspace1
bindsym $mod+Shift+2 move container to workspace $workspace2
bindsym $mod+Shift+3 move container to workspace $workspace3
bindsym $mod+Shift+4 move container to workspace $workspace4
bindsym $mod+Shift+5 move container to workspace $workspace5
bindsym $mod+Shift+6 move container to workspace $workspace6
bindsym $mod+Shift+7 move container to workspace $workspace7
bindsym $mod+Shift+8 move container to workspace $workspace8
bindsym $mod+Shift+9 move container to workspace $workspace9
bindsym $mod+Shift+0 move container to workspace $workspace10
bindsym $mod+Shift+minus move container to workspace $workspace11
bindsym $mod+Shift+equal move container to workspace $workspace12

bindsym $mod+Shift+Ctrl+1 move container to workspace $workspace1; workspace $workspace1
bindsym $mod+Shift+Ctrl+2 move container to workspace $workspace2; workspace $workspace2
bindsym $mod+Shift+Ctrl+3 move container to workspace $workspace3; workspace $workspace3
bindsym $mod+Shift+Ctrl+4 move container to workspace $workspace4; workspace $workspace4
bindsym $mod+Shift+Ctrl+5 move container to workspace $workspace5; workspace $workspace5
bindsym $mod+Shift+Ctrl+6 move container to workspace $workspace6; workspace $workspace6
bindsym $mod+Shift+Ctrl+7 move container to workspace $workspace7; workspace $workspace7
bindsym $mod+Shift+Ctrl+8 move container to workspace $workspace8; workspace $workspace8
bindsym $mod+Shift+Ctrl+9 move container to workspace $workspace9; workspace $workspace9
bindsym $mod+Shift+Ctrl+0 move container to workspace $workspace10; workspace $workspace10
bindsym $mod+Shift+Ctrl+minus move container to workspace $workspace11; workspace $workspace11
bindsym $mod+Shift+Ctrl+equal move container to workspace $workspace12; workspace $workspace12

bindsym $mod+Shift+Prior move scratchpad
bindsym $mod+Shift+Next scratchpad show



bindsym $mod+Ctrl+Shift+Right move container to workspace next
bindsym $mod+Ctrl+Shift+Left move container to workspace prev

bindsym $mod+Tab move workspace to output left

# Workspace assignments:

# Workspace 1 - file manager
assign [class="Doublecmd"] $workspace1
for_window [class="Doublecmd" title=".*Copying.*"] floating enable, move absolute position center
for_window [class="Doublecmd" title="Options"] floating enable, move absolute position center
for_window [class="Doublecmd" title=".*Deleting.*"] floating enable, move absolute position center


# Workspace 2 - text editor
#assign [class="Sublime_text"] $workspace2
assign [class="Atom"] $workspace2


# Workspace 3 - pycharm
assign [class="jetbrains-pycharm"] $workspace3

# Workspace 7 - Inkscape
assign [class="Inkscape"] $workspace7

# Workspace 7 - Gimp
assign [class="Gimp"] $workspace7


# Workspace 9 - browser
# SET UP BY SHORTCUT 'ff'

# Workspace 10  - tasks
assign [class="^Google-chrome$" instance="^calendar.google.com$"] $workspace10
assign [class="^Google-chrome$" instance="^trello.com$"] $workspace10
assign [class="^Google-chrome$" instance="^www.wunderlist.com$"] $workspace10
assign [class="^Google-chrome$" instance="^www.ticktick.com$"] $workspace10
assign [class="^Google-chrome$" instance="^www.onenote.com$"] $workspace10
assign [class="^nixnote2$"] $workspace10

# Workspace 11 - communication
assign [class="^Google-chrome$" instance="^www.gmail.com$"] $workspace11
assign [class="^Google-chrome$" instance="^outlook.office.com*"] $workspace11
assign [class="^Google-chrome$" instance="^inbox.google.com$"] $workspace11
assign [class="^Google-chrome$" instance="^web.whatsapp.com$"] $workspace11
assign [class="^Google-chrome$" instance="^molsysbio.slack.com.*"] $workspace11
assign [class="^Google-chrome$" instance="^teams.microsoft.com$"] $workspace11
assign [class="^Mailspring$"] $workspace11
assign [class="^Microsoft Teams - Preview$"] $workspace11


# Workspace 12 - music
assign [class="Rhythmbox"] $workspace12
assign [class="^Google-chrome$" instance="^www.soundcloud.com.*"] $workspace12
assign [class="^URxvt" title=".*cmus.*"] $workspace12
#assign [class="^Spotify$"] $workspace12

# Misc window floating rules

for_window [class="Galculator"] floating enable
for_window [class="Qalculate-gtk"] floating enable
for_window [class="vlc"] floating enable
for_window [class="steam" title=".*Steam Keyboard.*"] floating enable




# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

set $mode_system (l) Lock, (e) Logout, (s) Suspend, (h) Hibernate, (r) Reboot, (Shift+s) Shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3exit lock, mode "default"
    bindsym e exec --no-startup-id i3exit logout, mode "default"
    bindsym s exec --no-startup-id i3exit suspend, mode "default"
    bindsym h exec --no-startup-id i3exit hibernate, mode "default"
    bindsym r exec --no-startup-id i3exit reboot, mode "default"
    bindsym Shift+s exec --no-startup-id i3exit shutdown, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+e mode "$mode_system"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 5 px or 5 ppt
        bindsym Down resize grow height 5 px or 5 ppt
        bindsym Up resize shrink height 5 px or 5 ppt
        bindsym Right resize grow width 5 px or 5 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

#Email
bindsym XF86Mail exec inbox

#Calculator
bindsym XF86Calculator exec galculator

#Music player
bindsym XF86Tools exec rhythmbox

## Pulse Audio controls
#bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume `get_default_sink_name` +5% #increase sound volume
#bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume `get_default_sink_name` -5% #decrease sound volume
#bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute `get_default_sink_name` toggle # mute sound

# change volume or toggle mute
bindsym XF86AudioRaiseVolume exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+1 i3blocks
bindsym XF86AudioLowerVolume exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+1

bindsym $mod+Shift+KP_Add exec amixer -q -D pulse sset Master 5%+ && pkill -RTMIN+1 i3blocks
bindsym $mod+Shift+KP_Subtract exec amixer -q -D pulse sset Master 5%- && pkill -RTMIN+1 i3blocks
#bindsym XF86AudioMute exec amixer -q -D pulse sset Master toggle && pkill -RTMIN+1


# Sreen brightness controls
bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness

# Touchpad controls
#bindsym XF86TouchpadToggle exec /some/path/toggletouchpad.sh # toggle touchpad

# Media player controls
bindsym XF86AudioPlay exec player_control play-pause; exec cmus-remote -u
bindsym XF86AudioNext exec player_control next; exec cmus-remote -n
bindsym XF86AudioPrev exec player_control previous; exec cmus-remote -r

bindsym $mod+Shift+KP_Enter exec player_control play-pause; exec cmus-remote -u
bindsym $mod+Shift+KP_Multiply exec player_control next; exec cmus-remote -n
bindsym $mod+Shift+KP_Divide exec player_control previous; exec cmus-remote -r

# Lockscreen shortcut
# bindsym $mod+shift+x exec i3lock -c 000000

## Numlock/Capslock settings for i3blocks
#bindsym --release Caps_Lock exec pkill -SIGRTMIN+11 i3blocks
#bindsym --release Num_Lock  exec pkill -SIGRTMIN+11 i3blocks

# Misc keybindings
bindsym $mod+grave exec doublecmd
bindsym Print exec flameshot gui
bindsym Pause workspace $workspace9

# Widow Colours
#                         border  background text    indicator
  client.focused          $base00 $base00    $base05 $base08
  client.focused_inactive $base00 $base00    $base02 $base08
  client.unfocused        $base00 $base00    $base02 $base08
  client.urgent           $base00 $base02    $base05 $base08

bar {
    tray_output DP-0
    status_command i3blocks -c /home/carl/.config/i3/i3blocks.conf
    position bottom
    strip_workspace_numbers yes
    font pango: Hack Nerd Font 8
    #height 25
    #bindsym button4 nop
    #bindsym button5 nop
    colors {
      separator  $base01
      background $base00
      statusline $base05
      #                  border  background text
      focused_workspace  $base02 $base02    $base05
      active_workspace   $base00 $base00    $base04
      inactive_workspace $base00 $base00    $base04
      urgent_workspace   $base00 $base00    $base08
    }
}

# mouse and arrow key mapping to hjkl
set $normal 20
set $slow 1
set $fast 50
bindsym mod5+$left exec xdotool mousemove_relative -- -$normal 0
bindsym mod5+$down exec xdotool mousemove_relative 0 $normal
bindsym mod5+$up exec xdotool mousemove_relative -- 0 -$normal
bindsym mod5+$right exec xdotool mousemove_relative $normal 0

bindsym Ctrl+mod5+$left exec xdotool mousemove_relative -- -$slow 0
bindsym Ctrl+mod5+$down exec xdotool mousemove_relative 0 $slow
bindsym Ctrl+mod5+$up exec xdotool mousemove_relative -- 0 -$slow
bindsym Ctrl+mod5+$right exec xdotool mousemove_relative $slow 0

bindsym mod1+mod5+$left exec xdotool mousemove_relative -- -$fast 0
bindsym mod1+mod5+$down exec xdotool mousemove_relative 0 $fast
bindsym mod1+mod5+$up exec xdotool mousemove_relative -- 0 -$fast
bindsym mod1+mod5+$right exec xdotool mousemove_relative $fast 0

bindsym mod5+XF86Back exec xdotool click 1
bindsym mod5+space exec xdotool click 1
bindsym mod5+r exec xdotool click 3

bindsym mod5+KP_Add exec xdotool click 4
bindsym mod5+KP_Subtract exec xdotool click 5

#bindsym mod5+Shift+$left exec --no-startup-id xdotool key --window $(xdotool getactivewindow) Left
#bindsym mod5+Shift+$down exec --no-startup-id xdotool key --window $(xdotool getactivewindow) Down
#bindsym mod5+Shift+$up exec --no-startup-id xdotool key --window $(xdotool getactivewindow) Up
#bindsym mod5+Shift+$right exec --no-startup-id xdotool key --window $(xdotool getactivewindow) Right
#bindsym Mode_switch+space exec --no-startup-id xdotool key --window $(xdotool getactivewindow) Enter



exec_always --no-startup-id xmodmap -e "clear lock"
exec_always --no-startup-id xmodmap -e "keysym Caps_Lock = ISO_Level3_Shift"

exec_always --no-startup-id xmodmap -e "keysym Alt_R = Mode_switch"
exec_always --no-startup-id xmodmap -e "keycode  43 = h H Left H"
exec_always --no-startup-id xmodmap -e "keycode  44 = j J Down J"
exec_always --no-startup-id xmodmap -e "keycode  45 = k K Up K"
exec_always --no-startup-id xmodmap -e "keycode  46 = l L Right L"
exec_always --no-startup-id xmodmap -e "keycode  65 = space NoSymbol Return"
exec_always --no-startup-id xmodmap -e "keycode 118 ="

bindsym XF86Launch5 exec --no-startup-id "xmodmap -e 'keysym Mode_switch = XF86Launch1' && xmodmap -e 'keysym ISO_Level3_Shift = Mode_switch' && xmodmap -e 'keysym XF86Launch1 = ISO_Level3_Shift'"

# Startup apps
exec_always --no-startup-id feh --bg-fill "/home/carl/Pictures/9 - x6qNl8g.jpg"
exec_always amixer -q set Headphone 100 unmute
exec_always killall mate-notification-daemon
exec_always --no-startup-id dunst
exec_always --no-startup-id numlockx on

#exec --no-startup-id main_mon
#exec --no-startup-id set_to_dp
exec --no-startup-id mate-polkit
exec --no-startup-id /usr/bin/gnome-keyring-daemon --start --components=pkcs11,secrets,ssh
exec --no-startup-id picom --config ~/.config/picom.conf -b
exec --no-startup-id pulseaudio
exec --no-startup-id udiskie -s
exec --no-startup-id nm-applet
exec --no-startup-id xfce4-power-manager
exec --no-startup-id dropbox start
exec --no-startup-id insync start
exec --no-startup-id update-notifier
#exec --no-startup-id lighdmfix.sh
#exec --no-startup-id corsair
#exec /home/carl/bin/xrandr_scale


set $base00 #2b303b
set $base01 #343d46
set $base02 #4f5b66
set $base03 #65737e
set $base04 #a7adba
set $base05 #c0c5ce
set $base06 #dfe1e8
set $base07 #eff1f5
set $base08 #bf616a
set $base09 #d08770
set $base0A #ebcb8b
set $base0B #a3be8c
set $base0C #96b5b4
set $base0D #8fa1b3
set $base0E #b48ead
set $base0F #ab7967

#conda config --set auto_activate_base false